<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!--    <style name="Base.Theme.ICare" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <style name="Base.Theme.ICare" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your light theme here. -->
        <!-- 액션바 배경색 -->
        <item name="colorPrimary">@color/main_color</item>
    </style>

    <style name="Theme.ICare" parent="Base.Theme.ICare" />

    <!-- 메뉴 사이드 바를 만들기 위해 액션바 제거 -->
    <style name="Theme.NavigationDrawer" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="Theme.ICare.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ICare.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ICare.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>